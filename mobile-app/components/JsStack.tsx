// @ts-ignore - keep this thin wrapper without enforcing stack navigator types
import { Stack } from 'expo-router';

// Thin alias over expo-router's Stack to keep existing usage
export const JsStack = Stack;